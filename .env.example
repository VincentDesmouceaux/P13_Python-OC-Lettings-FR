## `.env.example`

```dotenv
# ─── Django (paramètres principaux) ───
DJANGO_DEBUG=false
DJANGO_SECRET_KEY=
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0,*.code.run
DJANGO_CSRF_TRUSTED_ORIGINS=https://*.code.run
DJANGO_BEHIND_PROXY=true

# ─── WhiteNoise / fichiers statiques ───
WHITENOISE_MANIFEST_STRICT=false

# ─── Logging ───
LOG_LEVEL=INFO

# ─── Sentry (optionnel) ───
SENTRY_DSN=
SENTRY_TRACES_SAMPLE=0.05

# ─── Application ───
PORT=8000
PY_VER=3.12
DOCKER_REPO=vincentdesmouceaux/oc-lettings-site
IMAGE_TAG=latest

# ─── Northflank API (déploiement) ───
NORTHFLANK_TOKEN=
NF_PROJECT_ID=
NF_OBJECT_ID=